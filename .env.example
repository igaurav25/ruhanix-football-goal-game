# Example configuration reference only.
# The current source still contains local development API URLs.
# Before deployment, move API configuration into environment-aware code.
API_BASE_URL=http://YOUR_LOCAL_IP/ruhanix_backend
